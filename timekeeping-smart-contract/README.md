# META NODE Timekeeping

### Setup For Unit Test
1. Clone source code
2. Run `npm i`
3. Run `make clone` for install dependencies of Foundry
3. Run `make unit-test`

* Reference: [Foundry Book](https://book.getfoundry.sh/forge/tests)